type Post {
    id: Int!
    description: String
    image: String
    user: User!
    modifiedDatetime: DateTime!
}

extend type Query {
    getPost(id: Int!) : Post,
    getFriendsPosts : [Post]!
}

extend type Mutation {
    createPost(
        description: String,
        image: String
    ) : Post
}